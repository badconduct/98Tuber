services:
  98tuber:
    build: .
    environment:
      PORT: 3000
      YOUTUBE_API_KEY: ${YOUTUBE_API_KEY:?set in .env}
      APP_DATA_DIR: /data
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./cache:/data/cache
    restart: unless-stopped
